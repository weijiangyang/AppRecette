<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">OnVaCusiner</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link " href="{{ path('app_index') }}">Accueil
                       
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link " href="{{ path('ingredient_index') }}">Mes ingrédients
                       
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link " href="{{ path('recipe_index') }}">Mes recettes
                       
                    </a>
                </li>

                <li class="nav-item">
                    <div class="nav-item dropdown px-5">
                        <a class="nav-link dropdown-toggle text-white" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Communauté de recettes</a>
                        <div class="dropdown-menu">
                            {% for category in category_service.findAll() %}
                                 <a class="dropdown-item" href="{{ path('recette_index_public',{'id': category.id}) }}">{{ category.name }}</a>
                            {% endfor %}
                           
                            
                            
                        </div>
                    </div>
                    {# <a class="nav-link " href="{{ path('recette_index_public') }}">Recettes de la commnauté
                       
                    </a> #}
                </li>
                
               
            </ul>
            {% if app.user %}
                <div class="nav-item dropdown px-5">
                        <a class="nav-link dropdown-toggle text-white" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user }}</a>
                        <div class="dropdown-menu">
                            
                            <a class="dropdown-item" href="{{ path('user_show',{'id':app.user.id}) }}">Mon profile</a>
                                {% if 'ROLE_ADMIN' in app.user.roles%}
                                    <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
                                {% endif %}
                          
                            <div class="dropdown-divider"></div>
                            
                            <a class="dropdown-item" href="{{ path('security_logout') }}">Deconnexion</a>
                        </div>
                </div>
            {% else %}
            <div class="d-flex">
                <a class="btn btn-primary" href="{{ path('security_login')}}">Connexion</a>
                <a class="btn btn-primary" href="{{ path('security_registration') }}">Inscription</a>
                       
                    
            </div>
                
            {% endif %}
            {# <div class="d-flex">
                <a class="btn btn-primary btn-sm" href="{{ path('security_login')}}">Connexion</a>
                <a class="btn btn-primary btn-sm" href="{{ path('security_registration') }}">Inscription</a>
                       
                    
            </div> #}
            
        </div>
    </div>
</nav>